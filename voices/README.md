# Voix des personnages

Un dossier par personnage (`alice/`, `arthur/`…), rempli par `tools/sync_from_forge.py`.
Non versionné : voir README à la racine.

Nommage : `<personnage>_ch<NN>_<II>.ogg` — traçable jusqu'au chapitre d'origine.
